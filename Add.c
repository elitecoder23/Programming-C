// Program that adds two numbers together

#include <stdio.h>

// Function that adds two integers together
int add(int a, int b) {
    return a + b; 
}

// Main function prints out the addition
int main() {
    printf("%d", add(50, 100));
    return 0;
}